route {
  path_prefix: "/a/sql"
  view: "sql_editor"
  require_module: "doc_sync"
  require_module: "sql_editor"
  require_module: "fn-codeeditor"
}

route {
  path_prefix: "/a/logviewer"
  view: "logviewer"
  require_module: "logviewer"
}

route {
  is_public: true
  path_match: "/a/login"
  view: "login"
  require_module: "login"
}

route {
  path_match: "/a/logout"
  view: "logout"
  require_module: "login"
}

route {
  path_match: "/a/"
  view: "appstore"
  require_module: "appstore"
}
